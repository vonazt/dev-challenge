type Query {
  allProducts(filter: String): [Product!]!
  product(id: ID!): [Product!]!
}

type Product {
  id: ID!
  name: String!
  price: Int!
  suppliedBy: String!
}

type Mutation {
  createProduct(name: String!, price: Int!, suppliedBy: String!): Product!
}
